import './App.css';

const base = process.env.REACT_APP_AIRTABLE_BASE
const key = process.env.REACT_APP_AIRTABLE_KEY

console.log(base)
console.log(key)

function App() {
  return (
    <div className="App">
    </div>
  );
}

export default App;
